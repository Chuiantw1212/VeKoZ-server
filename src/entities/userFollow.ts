export interface IUserFollow {
    // 未來呈現用戶列表功能
    followeeId?: string,
    followeeType?: string,
    followeeName?: string,
    followeeImage?: string,
    //
    id?: string,// userId
    name?: string,
    image?: string,
}